{
 "suite": "controller-core (CT-P1/CT-P3/CT-P5 deterministic lane, better-sqlite3)",
 "tests": {
  "pass": 12,
  "fail": 0
 },
 "negative_control": {
  "mutant": "drop-outbox-event",
  "pass": 11,
  "fail": 1,
  "killed_test": "SQL projection and pure reducer replay are trace-equivalent over a generated schedule",
  "verdict": "EFFECTIVE"
 },
 "coverage": [
  "contiguous LSN + monotone TypeSequence",
  "kill-point replay idempotency by operation identity",
  "digest conflict typed rejection",
  "status singleton (idempotent duplicate / typed conflict)",
  "stale-session fencing",
  "bounded admission fail-closed (payload/outbox-depth/tail) + overload recovery",
  "outbox exactly-once drain across repeated alarms",
  "batch all-or-nothing = per-record equivalence",
  "exact lookup typed NOT_FOUND (never EOF)",
  "fixed iterator head pinning",
  "SQL-vs-pure-reducer trace equivalence over seeded 200-event schedule",
  "reducer contiguity negative control"
 ],
 "workerd_runtime_validation": {
  "runner": "@cloudflare/vitest-pool-workers 0.21.3 (workerd via wrangler 4.123.0, pinned in source-lock)",
  "tests": {
   "pass": 2,
   "fail": 0
  },
  "facts": [
   "DatabaseControllerDO runs the shared ControllerCore over real DO SqlStorage + storage.transactionSync",
   "finalisation, replay idempotency, digest conflict, and status-singleton semantics verified inside the actual Durable Object runtime",
   "ControllerCore SCHEMA is the single source of truth for projection tables in both lanes"
  ]
 },
 "l1_local_stack": {
  "topology": "gateway Worker -> local R2 data path -> DatabaseControllerDO, served by wrangler dev (workerd)",
  "node_e2e": "18/18 PASS (scripts/local-stack-e2e.mjs): protocol 12 + outbox consumer loop 6 (at-least-once peek/ack, redelivery without ack, idempotent duplicate ack)",
  "rust_client": "tools/remote-wal-spike tests/l1_stack.rs 1/1 PASS - TB-P4 transport skeleton (ureq) drives upload/finalize/ambiguous-retry/digest-conflict 409/data-path 422/exact read/typed 404/fencing 409/contiguity against real workerd; self-boots the stack (process-group reaped, per-run port+namespace)",
  "outbox_consumer_contract": "peek returns unpublished without marking; ack after durable processing; lost ack => redelivery; dedupe key = control_seq"
 }
}
