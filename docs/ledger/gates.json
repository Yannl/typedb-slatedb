{
 "document": "Machine-readable gate / lane / contradiction / action ledger. This file is the SINGLE source of truth for gate state; docs/operations.md's gate table is rendered from it by tools/ledger/render_status.py and tools/ledger/lint_ledger.py rejects drift and forbidden green prose. A gate state here may only improve when an immutable evaluation over the CURRENT commit exists.",
 "authority_order": [
  "contract/AGENTS.md",
  "contract/typedb-r2-v17-final-addendum.md",
  "contract/typedb-r2-implementation-brief-v16.md",
  "v16 playbook / platform-probe spec / schemas / conformance plan",
  "compatible ADRs",
  "reviews, evidence summaries, operations notes, handoff prose"
 ],
 "current": {
  "document": "R8-P0-02: the ONE canonical current-fact section. Every current gate state, count, root and derived status lives HERE and nowhere else in this ledger. Any other field that restates one of these values is a second mutable copy of current truth and is refused by tools/ledger/lint_ledger.py (`guarded_patterns` below); prose that legitimately records what was believed at an earlier commit must carry historical: true, as_of_commit and superseded_by. Every value here is RE-DERIVED from the named evidence by tools/ledger/verify_ledger_semantics.py — a number typed into this file is not authority either.",
  "as_of_commit": "8014373a1c620b35b2cd984489e5ae17044bbb90",
  "derived_by": "tools/ledger/verify_ledger_semantics.py",
  "gates": {
   "G0": {
    "state": "OPEN",
    "blocking_findings": [],
    "blockers": [
     "raw artifact retrievability archive"
    ]
   },
   "G1": {
    "state": "OPEN",
    "blocking_findings": [
     "E-P0-08"
    ],
    "blockers": [
     "plan execution coverage (see current.coverage; the denominator is not a pass, and 9,196 of the remainder are U3/U4 product profiles that do not exist)",
     "official driver suites (E-P0-08): see current.drivers",
     "real Container resource/lifecycle + registry-provisioned ContainerDO (advisory-only today)"
    ],
    "owner_decisions": [
     "OD-021",
     "OD-022",
     "OD-023"
    ]
   },
   "G2": {
    "state": "NOT_READY_TO_EXECUTE",
    "blocking_findings": [
     "SI-G0-3"
    ],
    "blockers": [
     "owner-approved G2 numeric envelope file (absent by design until the owner signs one)",
     "SI-G0-3 credentials (including real managed key provisioning for the R4-PR1 issuer hierarchy)",
     "three OPEN product-conformance obligations in probes/obligations.ts"
    ],
    "owner_decisions": [
     "OD-008"
    ]
   },
   "G3_PLUS": {
    "state": "NOT_REACHABLE",
    "blocking_findings": [],
    "blockers": [
     "G0",
     "G1",
     "G2"
    ]
   }
  },
  "coverage": {
   "covered": 13723,
   "partial": 46,
   "uncovered": 9369,
   "total": 23138,
   "meaning": "COVERED means an outcome was RECORDED for that (leaf, profile, fixture set, toolchain) row. It never means the row passed.",
   "derived_by": "tools/qualification/leaf_coverage.py over exactly the bundles named below",
   "leaf_bundles": [
    "docs/evidence/G3/leaf/u0-full-1",
    "docs/evidence/G3/leaf/u1-full-1",
    "docs/evidence/G3/leaf/u1-http-2",
    "docs/evidence/G3/leaf/u2-full-1"
   ],
   "cucumber_bundles": [
    "docs/evidence/G3/leaf/cucumber-u0-1",
    "docs/evidence/G3/leaf/cucumber-u1-1",
    "docs/evidence/G3/leaf/cucumber-u2-1"
   ],
   "static_bundles": [
    "docs/evidence/G3/leaf/static-u0-1"
   ]
  },
  "mode_q": {
   "state": "VALID",
   "bundle": "docs/evidence/G0/mode-q",
   "bundle_root": "522dc2ea831bea564192ad6fdede487546d115e63effd9d2732852416678514a",
   "targets": 226,
   "bazel_version": "bazel 8.5.1",
   "bazel_binary_sha256": "61d89402f0368e64b6c827be5de79d8e65382e8124c3cbb97325611a1851392e",
   "derived_by": "tools/modeq/validate_modeq.py --dir docs/evidence/G0/mode-q"
  },
  "drivers": {
   "covered": 4,
   "partial": 2,
   "total": 6,
   "evidence": "docs/evidence/G1/drivers/driver-row-status.json",
   "derived_by": "tools/qualification/leaf_coverage.py driver rows (plan_coverage.driver_row_status)"
  },
  "guarded_patterns": [
   {
    "pattern": "\\b13[,.]?723\\b",
    "owner": "current.coverage.covered"
   },
   {
    "pattern": "\\b9[,.]?369\\b",
    "owner": "current.coverage.uncovered"
   },
   {
    "pattern": "\\b23[,.]?138\\b",
    "owner": "current.coverage.total"
   },
   {
    "pattern": "\\b226\\b",
    "owner": "current.mode_q.targets"
   },
   {
    "pattern": "\\b522dc2ea[0-9a-f]{8,}\\b",
    "owner": "current.mode_q.bundle_root"
   },
   {
    "pattern": "(?<![\\w-])G0\\b[^.\\n]{0,60}\\b(OPEN_RED|OPEN|CLOSED|NOT_READY_TO_EXECUTE|NOT_REACHABLE)\\b",
    "owner": "current.gates.G0.state"
   },
   {
    "pattern": "(?<![\\w-])G1\\b[^.\\n]{0,60}\\b(OPEN_RED|OPEN|CLOSED|NOT_READY_TO_EXECUTE|NOT_REACHABLE)\\b",
    "owner": "current.gates.G1.state"
   },
   {
    "pattern": "(?<![\\w-])G2\\b[^.\\n]{0,60}\\b(OPEN_RED|OPEN|CLOSED|NOT_READY_TO_EXECUTE|NOT_REACHABLE)\\b",
    "owner": "current.gates.G2.state"
   }
  ],
  "superseded_values": [
   {
    "pattern": "\\b914 of 23,?13[28]\\b",
    "was": "the round-6 leaf coverage numerator",
    "superseded_by": "current.coverage.covered"
   },
   {
    "pattern": "\\b22,?115\\b",
    "was": "the round-6 uncovered count",
    "superseded_by": "current.coverage.uncovered"
   },
   {
    "pattern": "Mode-Q evidence is absent|MODEQ: ABSENT|Mode-Q .{0,20}absent",
    "was": "the pre-SI-G0-1 Mode-Q state",
    "superseded_by": "current.mode_q.state"
   }
  ]
 },
 "adopted_audit": {
  "directive": "TypeDB × SlateDB × Cloudflare R2 round-5 deep audit and implementation directive, 2026-08-19",
  "audited_commit": "29fd0038c0fe1b3ddb6c9baebd6fadf2226c345c",
  "decision": "NO-GO for a live qualification run and NO-GO for a production claim; continue local implementation (R5 program adopted)"
 },
 "adopted_audit_round2": {
  "directive": "typedb-r2 main-branch deep audit and total-quality implementation directive, 2026-08-18",
  "audited_commit": "640c7d465fa83670f938d97b55d0d5f827adf946",
  "decision": "NO-GO for a product-labelled live Cloudflare qualification run"
 },
 "gates": [
  {
   "id": "G0",
   "title": "Source graph, identities, tools, fixtures, document truth",
   "authority": "v16 brief G0; v17 addendum Mode Q selection",
   "why": "SI-G0-1 IS CLOSED (2026-08-22). Mandatory Mode-Q Bazel cquery evidence now EXISTS and validates: docs/evidence/G0/mode-q, schema modeq-bundle/v1, bazel 8.5.1 (binary sha256 61d89402f0368e64b6c827be5de79d8e65382e8124c3cbb97325611a1851392e), the configured test targets crosswalked one-to-one into the canonical catalogue (count and bundle root: current.mode_q); tools/modeq/validate_modeq.py reports MODEQ: VALID and tools/modeq/modeq_mutants.py kills all 11 controls against that real bundle rather than a synthesised one. WHAT CHANGED, precisely: the blocker was never 'Bazel cannot run here'. aspect_bazel_lib REGISTERS bats_toolchains, and a registered toolchain must load before Bazel resolves toolchains for any configured target, so four archives fetched from github.com/bats-core/*/archive/<tag>.tar.gz - which this egress policy denies with 403 - gated the whole ANALYSIS phase. The same policy SERVES anonymous git reads of those repositories, and GitHub's release tarball is `git archive` of the tag, gzipped: measured 2026-08-22, `git archive --format=tar --prefix=<repo>-<ver>/ <tag> | gzip -n -6` reproduces all four BYTE-FOR-BYTE, matching the digests aspect_bazel_lib itself pins. tools/bazel/vendor_bats.py rebuilds them from the locked tags (now source-lock nodes BATS_CORE / BATS_SUPPORT / BATS_ASSERT / BATS_FILE) and REFUSES on any mismatch; Bazel verifies the same digests independently, so the reconstruction cannot forge a pass. With --distdir pointing at them, cquery analysed the whole configured test universe (current.mode_q.targets). The second wall recorded here still stands and is still worked around legitimately: //:deploy-mac-installer-pkg is an upstream mac-only alias whose select() has no default, so a whole-universe cquery fails on Linux regardless of network; the producer enumerates in the loading phase and configures only the resulting test labels. Two of the enumerated rules (//:Release_validate_deps_gen, //:release-validate-deps) are release plumbing, already carried as reviewed non-denominator targets by tools/bazel/bazel_parity.py, and the bundle records them as a declared exclusion rather than dropping them silently - which is why the crosswalked count in current.mode_q is two below the enumerated count. The remaining blocker, the raw artifact retrievability archive, is untouched by this work and is recorded in current.gates.G0."
  },
  {
   "id": "G1",
   "title": "Models, catalogue as qualification denominator, platform contracts, budgets, controller/container separation",
   "authority": "v16 brief G1",
   "why": "The DENOMINATOR exists: catalogue v2 with canonical shared identity and the immutable leaf/profile/fixture qualification plan v2 (23,132 rows + 6 driver rows) landed with R3-H, and the leaf-level evidence producer/validator chain landed with PR3/R4-D. CORRECTED 2026-08-22 (this paragraph said '0 rows fully covered' and 'the six official driver rows are NOT_IMPLEMENTED' long after both had stopped being true; a ledger that describes a state its own evidence contradicts is the failure this plane exists to prevent). MEASURED at fc2694b by tools/qualification/leaf_coverage.py over the seven archived bundles: the counts recorded in current.coverage. Four of the six official-driver rows are COVERED and two PARTIAL (R6-QUAL-01 + OD-010). What keeps G1 open is the REMAINDER, and the remainder is not mostly test execution: 9,196 of the uncovered rows are the U3 and U4 profiles, which are product code that does not exist (lane U3.0 is RED, U3_U4_PRODUCT is TYPED_UNAVAILABLE_BY_DESIGN); the Mode-Q crosswalk side of the plan is now PRODUCED and VALID (SI-G0-1 closed 2026-08-22, see G0); and the ContainerDO is advisory-only - no real Container resource/process or controller lifecycle integration exists (R3-F remainder; round-5 R5-SEC-06/07). Of the 13,942 rows reachable WITHOUT new product code, 219 remain, and NONE of them is execution work: 132 FAILPOINT rows at a granularity upstream does not emit (OD-022), 84 cucumber scenarios upstream's own runner filters by @ignore tag (OD-021), the 1 SCRIPT row whose script asserts nothing and whose Bazel target does not exist at the pin (OD-023), and the 2 PARTIAL driver rows (OD-010)."
  },
  {
   "id": "G2",
   "title": "Real-platform probe run under an owner-approved envelope",
   "authority": "v16 brief G2; platform-probe spec (14 probe IDs)",
   "why": "Round-4: the runner's destructive RED-preflight cleanup (R4-CF-00) is repaired with zero-authority refusal, lock-baseline restore and an enforced envelope meter (R4-CF-01), redaction is a serialization invariant (R4-CF-03), the nine /do|/ctr|/worker probes now have a deployable in-repo harness whose source digest is bound into the bundle (R4-CF-02, probes/harness-worker.ts + wrangler.probe-harness.toml), and every assertion is classified provider-fact vs product-conformance by the obligation manifest. ROUND-6 (R6-TRUTH-01 semantic pass, 2026-08-20 — what changed and what did NOT): (a) SELF-TEST COUNT: the harness now executes 26 controls, not 24. Executed 2026-08-20 on this branch: `bash probes/self-test.sh` → 'self-test: all 26 controls passed'. The 24 figure predated controls 7c and 7d. (b) ENVELOPE LIMITS: as of 09ce01e every signed envelope field now has an enforced global counter, including `max_cost_usd_cents`, which was previously signed and never priced; preflight refuses when the REMAINING signed validity is shorter than the credential TTL it would mint, and the displayed key fingerprint must equal the one derived from the trusted verification key. The claim 'all envelope limits are enforced' is TRUE for the LOCAL enforcement path, proved by budget-enforcement 9/9 including a schema-to-counter exhaustiveness test that fails when a signed field gains no counter. It is NOT a claim about provider-side enforcement: no credentialed run has ever occurred, so nothing here is attested against a real Cloudflare account. (c) AMBIGUITY: as of 3ac9ae9 all 16 mutation routes bind an explicit authoritative effect, IDEMPOTENT_REEXECUTE is gone from the union, and `deriveEffectOutcome` ends in `const unhandled: never` so a new variant cannot compile without an authoritative query. The four ambiguity cuts, restart resumption, one-physical-effect and byte-equal canonical replay are EXECUTED for every route. What that closes is the CODE-LEVEL obligation; the probe obligation `ambiguity-resolution-real` stays OPEN because it asks for the behaviour against a real Durable Object under real faults, which needs credentials. Recorded honestly with it: CUT_EXISTS/CUT_NOT_PENDING remain the typed refusals for a DIFFERENT token, and a reconstructed WAL receipt still differs in its `replayed` flag. (d) S3 EVIDENCE: as of 21780b0 the corpus bundle is schema v2 — clean-tree refusal BEFORE execution, provider digest RECOMPUTED from bytes and matched against the source lock, sealed lock bytes, structured per-phase results, and a path-independent stable root; evidence_mutants.py kills 28 of 28. The bundle is therefore SOURCE-AUTHENTIC and independently re-derivable. It is NOT 'qualification-grade' for the product: it certifies an S3 PROVIDER against the object-store corpus, not TypeDB against the upstream test corpus, and the two round-5 bundles are schema v1 and are REJECTED by the new verifier. The MinIO comparator bundle has not been regenerated. 'Attested' here means recorded-and-recomputable, not signed: there is no key infrastructure in that territory. Still blocking: no owner-approved envelope; no credentials; the production issuer/registry seam (R4-SEC-01) is implemented and proven locally (R4-PR1: opaque registry, provisioning transaction, mint/verify separation, managed-surface E2E) - what remains for live is provisioning real managed keys, under the credentials blocker; the three OPEN product-conformance obligations (ambiguity-resolution-real, runtime-cannot-delete, attempt-identity-changed-bytes-real)."
  },
  {
   "id": "G3_PLUS",
   "title": "Conformance / release gates beyond G2",
   "authority": "v16 playbook ordering",
   "why": "Earlier gates are open."
  }
 ],
 "lanes": [
  {
   "id": "U0",
   "state": "OPEN",
   "why": "Round 7 executed the first fresh pristine U0 over the current tree. docs/evidence/G3/leaf/u0-full-1 (executed, tree_state PRISTINE, tree_stable_across_run true, 106/106 targets, 0 refused, 0 timed out, 455 leaves: 452 PASSED / 2 FAILED / 1 IGNORED, root 1fb52e70dbd3507df8444f9500e5d0b8bd01e315390ceb9cdbd64bbe2cc5cf94) and docs/evidence/G3/leaf/cucumber-u0-1 (DERIVED from that bundle's archived bytes, nothing re-run; 47/47 features, 0 refused, 4071 leaves all PASSED, 28 scenarios NOT_RUN, root 13a82c609998e4325afc617b3d41dd4570e191d04933dafdbe66cd1f5b54baa7). Both verify CLEAN with 0 anomalies; u0-full-1 also verifies CLEAN under --corroborate-tree. Adding them carried 4,526 of U0's 4,740 plan rows to a leaf-granularity outcome; the resulting totals are current.coverage. The lane stays OPEN because 214 U0 rows remain uncovered (failpoint rows that libtest does not print per fail point, and static rows) and because COVERED records an outcome, never a pass: the 2 FAILED leaves are upstream's own todo!() stubs in storage/tests/test_recovery.rs, which the fork implements (upstream 7 #[test] with 2 todo!(); fork 18 #[test] with 0), and both pass under U1 and U2. ROUND 7h (2026-08-22): the STATIC_CHECK family got its lane. docs/evidence/G3/leaf/static-u0-1 is produced by tools/qualification/run_static_leaf.py and re-derived by tools/qualification/verify_static_leaf.py, which recomputes every checked file's digest against the pinned tree and RE-DERIVES each verdict rather than reading it (the tab and header predicates are deliberately reimplemented there; rustfmt is re-run). ROUND 8 (2026-08-22): re-sealed against the round-8 tree — 141 targets, 0 refused, 141 leaves all PASSED, root 673a5d252335ea5adccbbd69600961f98cb28a8780283e35bb7f3eb94e8385ab, 0 anomalies. The re-seal was not cosmetic: the first attempt reported 1 FAILED, because two files this round added to the fork (deny.toml, machete-baseline.json) carried no licence header. deny.toml has one now and the machete baseline moved to .quality/, where it belongs and where a JSON file is not asked to carry a comment header. tools/qualification/static_mutants.py holds 14/14 including the two R8-P2-01 portability controls (a relative bundle path, a non-default CARGO_HOME), and the verifier now REFUSES a tree-state mismatch by name instead of emitting 141 'has changed since the check read it' anomalies that a coverage run would read as absent evidence. Plan coverage 13,582 -> the counts in current.coverage."
  },
  {
   "id": "U1",
   "state": "OPEN",
   "why": "Re-run in round 7b against the CURRENT tree and superseding the round-6 bundles, which described a fork digest (4a163c9b) that the lint fixes in 6b68ac0 moved. docs/evidence/G3/leaf/u1-full-3: 106/106 targets, 0 refused, 0 non-zero exits, 0 timed out; 455 leaves = 454 PASSED / 0 FAILED / 1 IGNORED; tree_state FORK_STAGED_EXACT at fork digest 45912c8a; tree_stable_across_run true; root fbd0356029a0a03947e3ed01fb9155aab658343e2a8e71be23593876d986c989. docs/evidence/G3/leaf/cucumber-u1-3 derives 4071 leaves, all PASSED, from its archived bytes with nothing re-run (root 94659ed3…). Both verify CLEAN and leaf_mutants holds 16/16 against u1-full-3. CD-003 is answered: the oracle joins on leaf CASES, not target names. The 'pass BOTH u1 bundles as --oracle' trap is RETIRED — it existed because u1-full-1 refused four HTTP targets and u1-http-2 was their re-run; u1-full-3 refused nothing, so one bundle carries all 455 leaves. u1-full-1 and u1-http-2 are kept as valid evidence about the tree they were produced against. The lane stays OPEN because plan rows remain uncovered."
  },
  {
   "id": "U2",
   "state": "HISTORICAL_EXPERIMENTAL_NON_QUALIFYING",
   "why": "Round 7b: docs/evidence/G3/leaf/u2-full-3 is a CURRENT-tree SlateDB run — 106/106 targets, 0 refused, 455 leaves = 454 PASSED / 0 FAILED / 1 IGNORED, tree_state FORK_STAGED_EXACT at fork digest 45912c8a, root b03164af…, verify CLEAN, leaf_mutants 16/16 — with cucumber-u2-3 deriving 4071 leaves all PASSED (root 1111aadc…). Compared against the U1 oracle on the SAME tree and the same binaries, backend chosen at runtime: leaf_diff.py reports 0 regressions, 0 absent-on-candidate, 0 outcome-changed and 0 unexplained candidate-only over 455 leaf cases. That is the central result of this repository and it is now measured at HEAD rather than inherited. The lane state is UNCHANGED: this is single-node semantic parity evidence under OD-010's declared scope, not a conformance claim, and the earlier U2S3 archive it shares this row with remains historical observation only."
  },
  {
   "id": "U2S3",
   "state": "HISTORICAL_EXPERIMENTAL_NON_QUALIFYING",
   "why": "Classification: historical-dirty-tree-aggregate-smoke (label sidecar: docs/evidence/G3/u2s3-full-3/CLASSIFICATION.json). Archived run used a dirty 23-file staged tree (digest cc0d283a...); the current staged tree has 24 files and a different digest, and critical storage (cursor.rs, slate.rs, post_wal_guard.rs, storage.rs) changed after the run. Raw observation: 106 unique executable rows, 105 exited 0, 1 exited 101, 0 timeouts, 462 parsed passed, 2 parsed failed, 1 parsed ignored, 34 zero-case rows. That observation is preserved; it is NOT current-tree conformance and is never to be summarised as '105 green'.",
   "raw_observation": {
    "rows": 106,
    "exit_zero": 105,
    "exit_nonzero": 1,
    "timeouts": 0,
    "parsed_passed": 462,
    "parsed_failed": 2,
    "parsed_ignored": 1,
    "zero_case_rows": 34,
    "staged_tree_digest": "cc0d283a",
    "staged_file_count": 23
   }
  },
  {
   "id": "U3.0",
   "state": "RED",
   "why": "C-P0-01 is CLOSED: the Rust remote-WAL client was rewritten to the current protocol (PR4b 99038fc), threads the exact generation through every mint (R4-SEC-03 b53fd35) and speaks the R4-PR1 v2 provisioning protocol (e434761; live workerd suite 28/28). The lane stays RED because it is still a spike-level client lane: no product (TypeDB) integration consumes it (R4-F open item), the managed-surface lifecycle (reserve/attest/activate) is unimplemented (round-5 R5-SEC-02), and no current-tree U3.0 evidence bundle exists. Any prior U3.0 pass claim is stale."
  },
  {
   "id": "U3_U4_PRODUCT",
   "state": "TYPED_UNAVAILABLE_BY_DESIGN",
   "why": "Product profiles fail closed; the v17 per-database classic|slatedb-r2 seam is being built as refusal-first (S-P0-06). No product E2E exists."
  },
  {
   "id": "OFFICIAL_DRIVERS",
   "state": "OPEN",
   "why": "EXECUTING; 4 of 6 rows COVERED (R6-QUAL-01 + OD-010, 2026-08-20). All six official-driver rows run: 1,132 behaviour scenarios across Rust/Python/TypeScript on fork-classic (U1) and fork-slatedb (U2), 1,102 in-plan leaf outcomes, 0 failures, each bundle independently re-derived from its archived bytes by tools/evidence/verify_drivers.py, 99/99 mutants killed. The SlateDB backend passes the full official suites of all three drivers identically to RocksDB, evidenced by the archived on-disk witness _system/backend-spec.marker (kind slatedb-r2 / object-store local-fs vs kind classic), not by an environment-variable claim. OD-010 scopes the claim to single-node semantic parity: the cluster suites exercise multi-node replication, in which the storage adapter does not participate, so they are a DECLARED exclusion - still enumerated, still reported as not executed, with the authority named on every row - rather than a precondition holding rows partial for a reason unrelated to the property being qualified. No clustered, replicated or highly-available operation may be claimed on the strength of this. The four Rust and Python rows are therefore COVERED; the two TypeScript rows remain PARTIAL for genuine in-scope reasons: dependencies resolved by npm because the shipped pnpm-lock.yaml is v8 while local pnpm is 10.x, so the transitive closure is not the one the committed lockfile names, and upstream's http-ts emits no index.d.cts by its own BUILD TODO, so the Bazel type surface is not reproducible outside Bazel. Also recorded and NOT closed: driver/migration.feature and driver/cluster.feature have no plan leaves at all, because generate_catalog.py only enumerates feature files referenced from sources/typedb, so 18 executed migration scenarios cover nothing and are recorded as leaves_outside_plan."
  }
 ],
 "contradictions_registry": "docs/contradictions.json",
 "owner_decisions_registry": "docs/owner-decisions.json",
 "owner_decision_rule": "An OPEN owner decision cannot support a green gate. OD-005 (flake expiry placeholders) is OPEN; therefore any policy verdict that leans on the flake ledger is a development comparison, not final conformance.",
 "actions": [
  {
   "id": "PR0",
   "title": "Truth-only freeze and production containment",
   "status": "DONE",
   "items": [
    "gate ledger + linter + rendered status",
    "Mode-Q stop item reopened",
    "ADR-0002/ADR-0010 marked incompatible (CD-005/CD-006)",
    "legacy /session/register, /session/fence, local issuance, batch, budgets, outbox, audit excluded from the production surface",
    "legacy sessions without lifecycle state refused + terminal backfill migration",
    "U3/U4 remain typed-unavailable"
   ],
   "commits": [
    "db1a103",
    "20f542b"
   ]
  },
  {
   "id": "PR1",
   "title": "Rebuild G0 source/tool truth",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "OBJECT_STORE version/checksum reconciled",
    "TDRIVER tree recorded",
    "schema-driven per-kind lock validation + behavioral mutants",
    "Mode-Q evidence itself remains BLOCKED (no Bazel here) - gate stays OPEN_RED"
   ],
   "commits": [
    "db1a103"
   ],
   "status_note": "Mode-Q evidence itself remains BLOCKED (no Bazel here); G0 stays OPEN_RED"
  },
  {
   "id": "PR2",
   "title": "Catalogue v2 and immutable qualification plan",
   "status": "BLOCKED_WITH_LOCAL_PARTIALS",
   "items": [
    "canonical shared identity",
    "leaf/profile/fixture plan",
    "Mode-Q crosswalk",
    "driver namespaces"
   ],
   "commits": [
    "5d072c6"
   ],
   "status_note": "identity fail-closure + machine-readable denominator scope landed; the leaf/profile qualification plan's Mode-Q crosswalk is Bazel-blocked and driver namespaces need the driver lane"
  },
  {
   "id": "PR3",
   "title": "Immutable leaf-level evidence producer and validator",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "raw-log hashing + reparse binding",
    "bundle root + COMPLETE-last",
    "observation/policy split",
    "ledger fingerprint binding",
    "behavioral producer mutants",
    "per-leaf result mapping for composite suites remains OPEN"
   ],
   "commits": [
    "5d072c6"
   ],
   "status_note": "per-leaf result mapping for composite suites remains OPEN"
  },
  {
   "id": "PR4",
   "title": "Control-plane authority, migration, protocol",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "immutable DO database binding",
    "incarnation/generation-bound authority + rotation revocation",
    "historical migration matrix from old-schema fixtures",
    "canonical payload key binding",
    "framed journal v2 with database identity",
    "canonical u64 (no aliases) + checked incarnation",
    "pre-auth structural bounds",
    "capability outcome records",
    "full tenant registry + external recovery root remain OPEN (platform-blocked)"
   ],
   "commits": [
    "20f542b",
    "864e727"
   ],
   "status_note": "full tenant/environment registry, authenticated ingress and external recovery root are platform work (C-P0-02/11, C-P1-03/05/06/08/09 partials recorded in the response document)"
  },
  {
   "id": "PR4b",
   "title": "Rust L1 client rewritten to the current protocol",
   "status": "DONE",
   "closes": [
    "C-P0-01"
   ],
   "items": [
    "capabilities + decimal u64 + snapshotId + batch envelope client",
    "workerd integration test"
   ],
   "commits": [
    "99038fc"
   ]
  },
  {
   "id": "PR5",
   "title": "Storage containment and product seam",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "bounded SlateDB uploader (patch series)",
    "bounded post-WAL waits",
    "recovery singleton idempotence/quarantine",
    "checkpoint enumeration/fsync integrity",
    "supervised background tasks",
    "typed backend seam + BackendNotYetAvailable",
    "checked sequence exhaustion (partial)"
   ],
   "commits": [
    "60da7d8"
   ],
   "status_note": "uploader deadline/cancellation budgets, obligation-frontier redesign and registry-backed backend spec remain OPEN per the evidence file; slatedb patches 0002/0003 land with this commit"
  },
  {
   "id": "PR6",
   "title": "Fresh local baselines and official drivers",
   "status": "OPEN",
   "items": [
    "fresh U0/U1/comparison/drivers/U2/U2S3 in order against packaged binaries"
   ]
  },
  {
   "id": "PR7",
   "title": "CI as fail-closed truth plane",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "pinned action SHAs",
    "ledger/document-authority linter in CI",
    "new validators + mutants in CI",
    "full qualification jobs remain OPEN"
   ],
   "commits": [
    "0b407ff"
   ],
   "status_note": "SBOM/provenance/tested-equals-shipped await a release pipeline; fresh qualification is a dispatch-only job"
  },
  {
   "id": "PR8",
   "title": "Complete Cloudflare probe harness locally",
   "status": "DONE",
   "items": [
    "all 14 normative probe IDs",
    "fail-closed verdict aggregation",
    "mock/fault self-tests + mutants",
    "bundle root + COMPLETE-last",
    "owner envelope remains OPEN (owner input required)"
   ],
   "commits": [
    "36f3f9f"
   ]
  },
  {
   "id": "R3-A",
   "title": "Control-plane authority core (C-01,C-02,C-03,C-05,C-07)",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "C-01 authenticated journal migration + durable quarantine (re-sign hole closed)",
    "C-02 immutable capability outcomes, terminal replay, transition-quarantine",
    "C-03 auth framing before any DO contact",
    "C-05 controller-clock issuance + generation-bound finalize tokens",
    "C-07 stateless reads (no durable use row)",
    "REMAINDER: C-04 full tenant registry/private issuer (platform); C-02 cross-restart result store; C-05 remaining schema fields"
   ],
   "commits": [
    "048d46b"
   ]
  },
  {
   "id": "R3-B",
   "title": "WAL/recovery strictness (R-01,R-01b,R-02,R-07,R-09)",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "R-01 strict fixed-head contiguous recovery; two todo!() tests made real; flake exception removed",
    "R-01b authenticated CRC frames + torn-tail-only repair with forensic sidecar",
    "R-02 one status resolver for live and recovery",
    "R-07 checked sequence arithmetic (debug+release boundary matrix)",
    "R-09 bounded predecessor waits",
    "REMAINDER: R-08 crash-convergence assertions (subprocess-gated assembly lane); R-09 full hierarchical cancellation token"
   ],
   "commits": [
    "fbcb942"
   ]
  },
  {
   "id": "R3-C",
   "title": "Storage backend seam, shipping SlateDB, checkpoint safety (S-01..S-05,R-03..R-06,O-01)",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "S-02/S-03 patched SlateDB linked via [patch.crates-io] + external epochs + bounded uploader (storage lib 70)",
    "S-01 typed BackendSpec + persisted marker resolved before any touch; MaterialisationNamespace seam",
    "S-04 real immutability boundary (create-only/same-digest, journaled multipart, quarantine flag)",
    "S-05 no-compactor admission bound (OD-007)",
    "R-03 checkpoint off the automatic path (BackgroundTaskPolicy + task-inventory attestation)",
    "R-04 unique attempt id + safe sibling deletion + post-catchup scheduling",
    "R-05 scratch-first validate-before-activate restore + no-follow guard",
    "R-06 digest-bound COMPLETE + bottom-up fsync",
    "O-01 total/typed/budgeted metrics (TTL memo, single-flight, no fabricated counts)",
    "source-lock updated for the patched fork consumption (CD-007)",
    "storage lib 110, database 11, test_recovery 7, test_snapshot 10; every finding a killed mutant",
    "REMAINDER: R-08 crash-convergence assertions (subprocess-gated assembly lane); controller-provisioned namespaces need control-plane wiring; fsync crash-domain + MinIO LIST-fault not exercised here"
   ],
   "commits": [
    "8393d8f",
    "0530dc3",
    "cf1eb50",
    "3d75d38"
   ]
  },
  {
   "id": "R3-D",
   "title": "Publication firewall repair (F-01,F-02,F-03)",
   "status": "DONE",
   "items": [
    "F-01 single-guard gate (deadlock removed)",
    "F-02 authority on every mutating path",
    "F-03 typed manifest transitions",
    "ADR-0012 updated, no winner declared"
   ],
   "commits": [
    "0454115"
   ]
  },
  {
   "id": "R3-E",
   "title": "Probe safety, Mode-Q validator, CI and ledger truth (P-01..P-06,E-01,E-06,E-07)",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "P-01..P-06 secret separation/redaction, correct CF DTOs, record-before-dispatch, bundle v2, preflight",
    "E-01 Mode-Q semantic validator + 6 mutants",
    "E-06 CI job renamed honest + storage integration step",
    "E-07 ledger semantic checks + workspace-lock repo_commit removed",
    "REMAINDER: real fresh-execution U0 CI job; live probe execution (credential-gated)"
   ],
   "commits": [
    "1844c96"
   ]
  },
  {
   "id": "R3-F",
   "title": "Streaming data path + production-shaped ContainerDO (C-06,C-08)",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "C-06 DigestStream bounded PUT + streaming read verification + safe percent-decode",
    "C-08 advisory-only ContainerDO with identity fence, bounded ring, durable alarm GC (v2 migration)",
    "REMAINDER: true whole-response streaming vs inline base64 (P-WORKER); container process lifecycle (CF-02)"
   ],
   "commits": [
    "cc5c727"
   ]
  },
  {
   "id": "R3-G",
   "title": "Alchemy canonical stack + native MinIO supervisor (A-01)",
   "status": "DONE_WITH_RECORDED_REMAINDER",
   "items": [
    "A-01 pinned Alchemy graph + no-cloud guard + source-locked MinIO supervisor + graph/wrangler differential",
    "REMAINDER: Docker container lane (no daemon here)"
   ],
   "commits": [
    "767b387"
   ]
  },
  {
   "id": "R3-H",
   "title": "Catalogue v2 leaf plan + evidence bundle separation (E-02,E-03,E-04,E-05)",
   "status": "DONE",
   "items": [
    "E-02 immutable 23,132-row plan as the denominator; coverage reports PLAN NOT SATISFIED honestly",
    "E-03 U2S3 relabelled historical-dirty-tree-aggregate-smoke",
    "E-04 independent read-only verifier + policy_roots pinning + 9 mutants",
    "E-05 driver rows recorded NOT_IMPLEMENTED"
   ],
   "commits": [
    "7f2ba55"
   ]
  },
  {
   "id": "R3-I",
   "title": "Docs/ledger truth pass, final review, sweep, ship",
   "status": "DONE",
   "items": [
    "round-3 response document",
    "ADR-0009/spec-delivery/handoff §18 corrections",
    "ledger round-3 adoption",
    "source-lock truth for the shipped fork (CD-007)",
    "evidence reconciliation after R3-B flake-ledger change (bundle root re-sealed, all 5 evidence checks green)",
    "factorization/simplification pass: behavior-preserving dedup across control-plane, tooling/stack, storage/recovery/WAL, and the firewall spike; one graph-diff rigor gap closed with a new mutant; deliberate producer/verifier independence left intact; each lane re-verified",
    "full-suite sweep green: source-lock lint, ledger lint, modeq-consistent, evidence chain (bundle root 28ae4b7e, 0 anomalies), control-plane (82/19/E2E-85), stack 31, storage 110 + integration, durability + database"
   ],
   "commits": [
    "273d0d5",
    "11401b2",
    "af83476",
    "765c4e9",
    "3d75d38",
    "0fcde86",
    "e3b3632",
    "b1f574a",
    "e054e45",
    "4397747",
    "92573cb"
   ]
  },
  {
   "id": "R4-A",
   "title": "Destructive-stop safety: zero-authority refusal, lock baseline restore, enforced envelope, redaction invariant (R4-CF-00/01/03)",
   "status": "DONE",
   "closes": [
    "R4-CF-00",
    "R4-CF-01",
    "R4-CF-03"
   ],
   "items": [
    "RefusedProvider + zero-call assertion",
    "lock snapshot/conservative restore, never rules:[]",
    "MeteredProvider budget-before-dispatch + intent journal + verdict-after-cleanup",
    "write-time deep redaction + seal-time secret scanner",
    "runner-safety.test.ts 14 mutants incl. the audit's reproduced counterexample; self-test control 7b"
   ],
   "commits": [
    "d89b01d"
   ]
  },
  {
   "id": "R4-B",
   "title": "Posture truth: managed default deploy, explicit local config, per-graph posture invariants, migration ledger (R4-STACK-01/02/08/10)",
   "status": "DONE",
   "closes": [
    "R4-STACK-01",
    "R4-STACK-02",
    "R4-STACK-08",
    "R4-STACK-10"
   ],
   "items": [
    "cloudflare-real graph emits managed vars; posture invariants validated per graph",
    "wrangler.toml = managed fail-closed default; wrangler.local-dev.toml explicit",
    "alchemy.run.ts execution-mode assertion (dev-only mechanically)",
    "binding diff by stable name; bucketName only on PAYLOADS",
    "append-only migration ledger enforced in both configs"
   ],
   "commits": [
    "231d719"
   ]
  },
  {
   "id": "R4-C",
   "title": "Storage: multipart immutability bypass, manifest requirement, checkpoint ordering/fallback, backend identity (R4-STOR-00..11)",
   "status": "DONE",
   "closes": [
    "R4-STOR-00",
    "R4-STOR-01",
    "R4-STOR-02",
    "R4-STOR-04",
    "R4-STOR-07",
    "R4-STOR-08",
    "R4-STOR-10",
    "R4-STOR-11"
   ],
   "items": [
    "R4-STOR-04/07: multipart mutation policy enforced at use time; checkpoint refuses without a manifest root",
    "R4-STOR-08/10/11: recency-aware retention, newest-to-oldest checkpoint fallback, no fixture on remote startup",
    "R4-STOR-00: BackendContext resolved once per open; mid-process env change = typed refusal (barrier mutant)",
    "R4-STOR-01: backend-identity v2 = FULL identity (incl. non-secret S3 binding) digest-sealed; checkpoints carry BACKEND_IDENTITY, cross-identity restore is a HARD typed refusal",
    "R4-STOR-02: durable no-follow marker write (create_new tmp + hard_link publication + parent fsync); stray attempt dirs removed only when empty and attempt-owned",
    "storage lib 138 + test_recovery 11 + database suites green"
   ],
   "commits": [
    "5aff4ff",
    "0da6d0b",
    "ca1e5cd"
   ]
  },
  {
   "id": "R4-D",
   "title": "Evidence adjudication split, source-lock freshness, CI v2 enforcement, Mode-Q semantics (R4-EVID-01/01a/03a, R4-MODEQ-01)",
   "status": "DONE",
   "closes": [
    "R4-EVID-01",
    "R4-EVID-03a",
    "R4-MODEQ-01"
   ],
   "items": [
    "R4-EVID-03a: verify_all.py separates observation_integrity_verified / policy_adjudicated / qualification_pass; exact verdict enum, plan root recomputed from canonical bytes; six diligent-forger mutants added (15/15)",
    "R4-EVID-01a: validate_catalog.py + build_plan_v2.py --check always compare against the CURRENT source lock; --require-current-lock is the hard form (the finding ITSELF - the stale committed catalogue/plan digest - stayed OPEN at this commit and is closed by the round-5 regeneration, R5-PR0)",
    "R4-EVID-01: CI truth-plane runs the full v2 chain incl. plan_coverage as an EXPECTED-RED gate (must exit 1 and regenerate byte-identically)",
    "R4-MODEQ-01: validate_modeq.py enforces argv grammar, cquery stdout grammar, current workspace-lock equality, crosswalk bijection, safe basenames; 11/11 mutants killed"
   ],
   "commits": [
    "868f617"
   ]
  },
  {
   "id": "R4-E",
   "title": "Security seam: exact capability actions, read fencing, Rust finalize generation, checkpoint activation proof (R4-SEC-03..06)",
   "status": "DONE",
   "closes": [
    "R4-SEC-03",
    "R4-SEC-04",
    "R4-SEC-05",
    "R4-SEC-06"
   ],
   "items": [
    "R4-SEC-04: closed capability method registry, exact per-action methods with REQUIRED restrictions; unknown method refused (CAPABILITY_METHOD_UNKNOWN)",
    "R4-SEC-05: assertActiveReader — WAL_READ binds session+generation and is revalidated live at use time",
    "R4-SEC-03: Rust L1 client threads the exact generation through every mint (no default/zero fallback); bound actor; validating contract mock; live workerd 24/24",
    "R4-SEC-06: checkpoint activation takes the material checkpoint-restore-evidence/v2 manifest, validated field-by-field",
    "R4-PR1 / R4-SEC-01 (locally executable): opaque tenant registry + PROVISION transaction (first-call squat dead, race loser typed PROVISION_CONFLICT), registry-derived DO routing (idFromName from the VERIFIED token binding), private-issuer key hierarchy (root never reaches runtime; per-scope derived keys; capability-scope material cannot mint PROVISION), token schema v2 (v/kid/env/tenantId, closed field set), per-run ephemeral managed keys, managed-surface E2E (dev-route 404 matrix with zero side effects + full internal bootstrap). Node 96/96, workerd 25/25, e2e 94 PASS, e2e:managed 23 PASS.",
    "Rust client speaks the provisioning protocol: byte-exact v2 PROVISION mint pinned to the TS implementation by a cross-implementation vector; live workerd suite 28/28.",
    "Still live-blocking (not locally provable): real managed key provisioning/rotation drills at deploy time; asymmetric upgrade seam recorded in registry.ts."
   ],
   "commits": [
    "56fda81",
    "b53fd35",
    "a8adfff",
    "e434761"
   ]
  },
  {
   "id": "R4-G",
   "title": "Probe obligation manifest + deployable harness (R4-CF-02/04)",
   "status": "DONE",
   "closes": [
    "R4-CF-02",
    "R4-CF-04"
   ],
   "items": [
    "R4-CF-02: deployable in-repo probe harness (probes/harness-worker.ts + wrangler.probe-harness.toml) for the nine /do|/ctr|/worker probes; harness source digest bound into the bundle",
    "R4-CF-04: versioned obligation manifest (probe-obligations/v1) classifies all 100 assertions provider-fact vs product-conformance; three product obligations recorded honestly OPEN; exact-set reconciliation is RUN-INVALID on dangling refs/unclaimed assertions; VERDICT.json carries sub-verdicts so mock evidence can never satisfy a product obligation"
   ],
   "commits": [
    "6301fec"
   ]
  },
  {
   "id": "R4-F",
   "title": "Local stack: semantic readiness, PID identity, transitive guard, secure downloader/secrets, S3 certification corpus, native-fidelity topology (R4-STACK-03..09, R4-LOCAL-05..07, PR2/PR3)",
   "status": "IN_PROGRESS",
   "items": [
    "R4-STACK-04/05/06/09 + R4-LOCAL-05/06/07: semantic readiness, PID identity, transitive no-cloud guard, hardened downloader, secret hygiene (committed e37f9e8)",
    "PR2: provider-neutral S3 certification corpus EXECUTED GREEN on both providers (MinIO locked baseline AND pinned RustFS 1.0.0-rc.2): 9 semantic tests + kill -9 crash/restart persistence via object_store =0.14.1",
    "OD-009: RustFS pinned in source-lock (zip dbe7b718…, binary d286e5f8…, commit e9f53180 cross-checked two ways); corpus runner hard-refuses digest mismatch",
    "PR3 (partial): deterministic fault proxy (tested, 7 checks) + native-fidelity lane stack/native-fidelity.mjs EXECUTED GREEN on both providers: real typedb_server_bin over SlateDB-on-S3 through the proxy, official HTTP workload, kill -9 recovery, parallel isolation, injected-fault survival, and the U1 local-fallback witness mutant",
    "OPEN: remote-WAL (U3) product integration and TypeDB-driver-level suites in the lane; L2 container parity"
   ],
   "commits": [
    "e37f9e8",
    "a3411c8",
    "f685d0e",
    "8809f89",
    "f856791",
    "b170481"
   ]
  },
  {
   "id": "R5-PR0",
   "title": "Round-5 PR0: truth plane current and fail-closed (R5-REL-01, R5-EVID-01, R5-QUAL-01, R5-CI-01 workflow hardening)",
   "status": "DONE",
   "items": [
    "R5-REL-01: rendered status regenerated; ledger semantic contradictions reconciled (stale G1/U3.0/q_disposition text); linter extended with DONE-without-commit, closed-finding-vs-blocking-finding contradiction, and committed-status-regression checks; tools/ledger/ledger_mutants.py executes and kills each mutant",
    "R5-EVID-01: catalogue + plan v2 regenerated from the pristine pinned tree against the CURRENT source lock (837d52fb...); --require-current-lock is now a HARD CI failure (both || echo fallbacks removed); plan coverage remains honestly RED",
    "R5-QUAL-01: dispatch storage lane split into `cargo test -p storage --tests --lib --locked` + `cargo bench -p storage --no-run --locked` so the bench CLI can no longer panic a qualification command",
    "R5-CI-01 (workflow hardening only): top-level `permissions: contents: read`, persist-credentials: false on every checkout, per-job timeouts, locked local node binaries instead of npx; the release pipeline itself (tested-equals-shipped, SBOM/provenance) remains the recorded PR7 remainder"
   ],
   "commits": [
    "0268b60"
   ]
  },
  {
   "id": "R5-PR1",
   "title": "Round-5 PR1: executable managed configuration and asymmetric trust boundaries (R5-SEC-01, R5-SEC-03, R5-SEC-08)",
   "status": "DONE",
   "items": [
    "R5-SEC-03: capability + provision tokens are Ed25519 schema v3; the managed runtime holds ONLY public verification keys (mutant: stealing every managed env var yields no minting ability); minting lives solely in the issuer module; two-slot rotation keyring with explicit retirement; journal MAC deliberately stays symmetric (writer=verifier=same DO), documented",
    "R5-SEC-01: managed inputs resolved from ONE requirement source consumed verbatim by both the runtime resolver and the pre-deployment graph checker; construction test boots from exactly the graph-declared names; drop-each-input mutants fail before deployment",
    "R5-SEC-08: docs/security-topology.md — principal/binding/key map, rotation per key, and the explicit statement that TypeDB end-user auth is NOT service auth, every claim citing its enforcing file",
    "Verified: tsc clean; node 108/108; workerd 27/27; e2e + e2e:managed ALL PASS; stack 64/64; wrangler consistency PASS"
   ],
   "commits": [
    "9d60e53"
   ]
  },
  {
   "id": "R5-PR2",
   "title": "Round-5 PR2: control lifecycle, ambiguity resolution and service integration (R5-SEC-02, R5-SEC-04, R5-SEC-06, R5-SEC-07, R5-SEC-09)",
   "status": "DONE",
   "items": [
    "R5-SEC-04: capability uses are a durable operation/effect protocol — each claim records its authoritative effect, a recovery reducer settles AMBIGUOUS from that effect (present->exact original success, absent->re-execute, contradicted->QUARANTINED terminally), the retry path settles instead of answering 409 forever, and restart/alarm sweeps converge without any client retry. 10 mutants executed (7 core + 3 workerd)",
    "R5-SEC-02: the exact Rust client operates the MANAGED surface as a pure bearer of issuer-granted v3 tokens (no local minting); new managed lane boots wrangler.toml + the private issuer sidecar and proves the dev-route 404 matrix from Rust before any bootstrap. 44 checks on the managed lane, 35 on local-dev, contract 17, lib 14",
    "R5-SEC-06: ContainerDO is registry-provisioned only (two-tenant race, squat, wrong principal, stale incarnation, unbound observation mutants); R5-SEC-09: observation bytes bounded per field, per request on ACTUAL bytes, and in aggregate",
    "R5-SEC-07 (locally executable part): the graph marks the container execution facet declared-ahead/advisory, enforced by a stack honesty test; the DO records a typed containerRuntime descriptor so the future real Container resource binds an exact image identity. No actual Container process exists — recorded openly",
    "Residual recorded: drain/revoke, checkpoint open/activate and the authenticated ambiguous-operation query are not yet in the Rust client"
   ],
   "commits": [
    "7de23b8",
    "48bf2c3",
    "197547c"
   ]
  },
  {
   "id": "R5-PR3",
   "title": "Round-5 PR3: storage configuration, product seam and fencing as shipped behaviour (R5-STOR-01..05, R5-STOR-08, R5-STOR-10)",
   "status": "DONE",
   "items": [
    "R5-STOR-01: BackendContext owns the complete effective S3 configuration; the static OnceLock and every env read below admission are deleted, with a source-level guard test",
    "R5-STOR-05: multipart completion is provider-atomic create-only (staging key -> readback verify -> atomic promote); two independent completers yield one winner and no changed-byte overwrite",
    "R5-STOR-08: journal retires terminal rows to a bounded receipt window with hard attempt/byte admission budgets",
    "R5-STOR-10: silent legacy rebind is dead on both the marker and checkpoint sides; explicit imports require the acknowledged full identity and write sealed provenance",
    "R5-STOR-04: the strict external-epoch fence SHIPS (unconditional on the storage crate's slatedb dependency, attested by cargo tree). The honest suite gate asserts that under the shipped posture EVERY failing upstream test fails BECAUSE the fence refused an unauthorized epoch-less open — which caught 3 genuine regressions hidden among 423 expected refusals (patch 0005)",
    "R5-STOR-02: classic|slatedb-r2 is a product option resolved from server configuration; a conformance profile can never silently decide it (typed ProductBackendProfileMismatch); R5-STOR-03: the local epoch source documents what it is not, and the lanes that would claim cross-host fencing refuse at admission",
    "Verified: storage lib 180/0, test_recovery 18/0, durability 34/0, database 21/0; strict-epoch suite gate PASS"
   ],
   "commits": [
    "46164cd",
    "5cbe3dd",
    "e63a337"
   ]
  },
  {
   "id": "R5-PR4",
   "title": "Round-5 PR4: atomic object and recovery safety (R5-STOR-06, R5-STOR-09, R5-STOR-11, R5-STOR-12)",
   "status": "DONE",
   "items": [
    "R5-STOR-06: restore materialises into scratch, re-verifies, opens every keyspace, replays the fixed WAL head, and only then atomically activates; the kill-point matrix (before/during/after activation, unopenable cut) is executed and every pre-activation failure leaves the predecessor byte-identical AND active",
    "R5-STOR-09: CHECKPOINT-COMPLETE v2 — streaming SHA-256 per file and root over length-prefix-framed raw path bytes; lossy-path collisions impossible; v1/64-bit manifests refused as unverifiable",
    "R5-STOR-11: attempt ordering and retention keyed by the sealed checkpoint sequence, never wall clock; clock-rollback and renamed-attempt mutants",
    "R5-STOR-12: the reachable todo!() in open_snapshot_write_at is a typed error"
   ],
   "commits": [
    "3e6ed64"
   ]
  },
  {
   "id": "R5-PR5",
   "title": "Round-5 PR5: local production-parity evidence (R5-LOCAL-01, R5-LOCAL-03) and the signed live envelope (R5-CF-01)",
   "status": "DONE",
   "items": [
    "R5-LOCAL-01: the CAS gate can no longer accidentally serialize — barrier-released rounds (100x16 independent-client writers, 1 MiB pairwise-distinct bodies), current-ETag update races with post-race ABA refusal, and a multi-PROCESS lane (8 OS processes per round, start-file barrier, winner self-verifies byte-exact stored bytes). Executed green on BOTH pinned providers",
    "R5-LOCAL-03: every corpus run seals a structured evidence bundle (provider binary digest, per-phase logs+digests, crash receipt, root seal) verified by an independent verifier",
    "R5-CF-01: the approval envelope is a SIGNED, run-bound, one-time authorization artifact (Ed25519 over the canonical body, verified against an out-of-band public key, bound to release commit/probes source root/account/bucket/nonce/one run id, validity <=7 days, consumed at authority acquisition). 16 mutants executed + self-test control 7c",
    "R5-LOCAL-02 remains OPEN: the single-command ladder combining managed Worker/DO + Rust client + TypeDB + RustFS + official drivers is not built"
   ],
   "commits": [
    "f8fa481",
    "0d111e9",
    "660c211"
   ]
  },
  {
   "id": "R5-REL-00",
   "title": "Round-5 release truth: the audited implementation is ON main at a recorded commit (R5-REL-00)",
   "status": "DONE",
   "items": [
    "PR #8 merged to main as 553febdec5c1cca6e7955b0971feeb6aa9f57046 (merge commit; branch head c0e8082).",
    "TREE EQUALITY VERIFIED: `git diff HEAD origin/main` is EMPTY — the merged tree is byte-identical to the tree every gate in the final sweep was executed against, so no merge delta invalidates the evidence.",
    "Commit-sensitive gates re-executed ON the merged commit: ledger lint PASS, ledger mutants 16/16 killed (including done-action-citing-non-ancestor-commit, which verifies every commit this ledger cites exists in main's history), catalogue --require-current-lock PASS, plan --check --require-current-lock PASS (23132 rows).",
    "Gate states unchanged by the merge and honest: G0 OPEN_RED, G1 OPEN, G2 NOT_READY_TO_EXECUTE, plan coverage 0/23138."
   ],
   "commits": [
    "553febd"
   ]
  },
  {
   "id": "R6-PR8-SUPPLY",
   "title": "Round-6 R6-SUPPLY-01: the stack dependency surface is advisory-clean and the gate is machine-readable",
   "status": "IN_PROGRESS",
   "items": [
    "MEASURED BEFORE: `cd stack && npm audit --omit=dev` = 14 advisories (9 high, 5 moderate) over 307 production-class packages, 43 distinct GHSA ids.",
    "The audit's prescribed remedy - 'upgrade to the newest compatible Alchemy line' - was NOT AVAILABLE: alchemy@2.0.0-beta.72 IS the newest published line (dist-tags latest AND next, published 2026-08-12). The only semver-greater registry versions are April-2026 pipeline/CLI test tags, which is why `npm audit fix --force` proposed the four-months-OLDER 2.0.0-pipeline-v2-test. Recorded as a finding about the recommendation, not papered over.",
    "REMEDIATED INSTEAD at the transitive packages with pinned npm `overrides` in stack/package.json: @hono/node-server 1.19.9->1.19.17, hono 4.11.4->4.13.3, lodash 4.17.21->4.18.1, valibot 1.2.0->1.4.2, sharp 0.34.5->0.35.3, @puppeteer/browsers 2.13.2->3.2.1 (which drops extract-zip entirely - the only package with NO fixed version at any release).",
    "MEASURED AFTER: 0 advisories, 247 production-class packages. `node cli.mjs graph` is BYTE-IDENTICAL before and after; `node cli.mjs check-wrangler` PASS; the stack suite passes.",
    "GATE: tools/ci/check_npm_advisories.py + tools/ci/dependency-advisory-policy.json. A reported advisory needs an unexpired exception carrying advisory id, package, affected range, reachability, reason, compensating control, owner and expiry <= 90 days. Expired, unbounded, stale, incomplete and brand-new-unlisted all FAIL. --lockfile additionally requires every locked package to resolve from an allowed registry with an integrity digest.",
    "The exception list is EMPTY because nothing needed excepting. The gate's 11 behavioural mutants prove it would fail if something did."
   ]
  },
  {
   "id": "R6-PR8-CI",
   "title": "Round-6 R6-CI-01: CI is three tiers, and ENVIRONMENT-BLOCKED is a distinct non-green state",
   "status": "IN_PROGRESS",
   "items": [
    "TIER 1 (.github/workflows/gates.yml, every push/PR) gains: a `hygiene` job (rustfmt on the tools workspace AND the TypeDB fork under the pinned nightly, `git diff --check` over the PR range and the working tree, clippy -D warnings on all three tools crates, the warning-ratchet mutants); a `supply-chain` job (advisory policy + lockfile provenance + their mutants, the dependency-source linter's mutants, the release-identity template check and its four-posture self-test, the capability-probe mutants); a short RustFS provider corpus with its capability probe; and, in the storage job, the durability unit lane, the shipped-fence attestation, the STRICT negative epoch suite (--quick) and the dependency-source truth gate.",
    "TIER 2 (.github/workflows/nightly.yml, NEW - nightly + manual pre-live) carries what previously ran nowhere or dispatch-only: storage/durability integration tests, bench compile-check, the FULL feature-off + feature-on fence suite, the full RustFS crash/fault corpus with the MinIO comparator and the evidence mutants, managed local Node and Rust E2Es, the pristine catalogue drift rebuild, npm + cargo advisory scans, and a ledger-honesty gate that turns red if a driver harness lands without the OFFICIAL_DRIVERS lane being updated.",
    "TIER 3 (.github/workflows/release-qualification.yml, NEW - tag or explicit dispatch only): clean-tree refusal before anything is measured, an immutable digest-bound release identity, the hermetic truth plane, SBOM (npm CycloneDX) + resolved-graph cargo inventory + digests bound to the identity, and a final release-predicate job that evaluates EVERY gate and lane and fails while any is unmet. Today it fails by construction: the gate states recorded in current.gates.0 is RED, OFFICIAL_DRIVERS is NOT_IMPLEMENTED.",
    "NO `continue-on-error:` key exists on any job or step in any of the three workflows.",
    "ENVIRONMENT-BLOCKED is IMPLEMENTED, not described: tools/ci/capability_probe.py classifies the runner, the lane runs only when CAPABLE, and a dedicated `*-environment-blocked` job FAILS with exit 75 (EX_TEMPFAIL) and an `::error title=ENVIRONMENT-BLOCKED::` annotation naming the missing capability and the runner needed. `continue-on-error` was rejected because it reports SUCCESS; a bare `if:` was rejected because it reports SKIPPED, which branch protection also treats as satisfied.",
    "RECORDED REMAINDER: no SHA-pinned artifact-upload action exists in this repository, so blocked verdicts and attestation digests are written to the job summary rather than uploaded. Adding an artifact action requires resolving and recording its commit SHA, which this session could not verify."
   ]
  },
  {
   "id": "R6-PR8-TRUTH",
   "title": "Round-6 R6-TRUTH-01: the SlateDB fork decision is ratified honestly, and a semantic linter keeps it that way",
   "status": "IN_PROGRESS",
   "items": [
    "GROUND TRUTH, re-derived: `cargo metadata` on the STAGED sources/typedb workspace resolves exactly one slatedb node, path+file://.../sources/slatedb-fork#slatedb@0.15.0, with features [aws, external_epoch_required, wal_disable]. The tools workspace (no patch table) resolves registry+https://github.com/rust-lang/crates.io-index#slatedb@0.15.0.",
    "The supersession is WIDER than ADR-0012 originally ratified: [patch.crates-io] is workspace-global by construction (Cargo has no per-lane patch table), so U2/U2S3 and even the RocksDB oracle lanes resolve the fork. ADR-0012's status block now ratifies that explicitly and marks its decision 3 (conformance lanes keep crates.io) SUPERSEDED; the oracle property is preserved differently - tools/storage-diff-spike links the unpatched registry crate from a workspace with no patch table.",
    "RATIFIED ACROSS: ADR-0001 (status SUPERSEDED in full, title marked, round-6 banner), ADR-0012 (status accepted (shipped), scope table, oracle explanation, fence default-on, upgrade/rebase pointer, risk ownership), the ADR index, README, docs/architecture.md, docs/development.md (a new fork/slatedb patch-discipline section plus an upgrade/rebase procedure and a named risk owner), docs/operations.md, the arcadia EPBS and physical-architecture dependency tables, docs/design/slatedb-external-epochs.md and docs/spec-delivery-comparison.md.",
    "GATE: tools/ci/check_dependency_sources.py + tools/ci/dependency-source-claims.json. SEMANTIC, not grep - it reads cargo metadata's resolve graph, fails when the graph contradicts a ratified claim, and only THEN checks that every registered document states the posture the graph proves (both forbidden prose and required statements). Executed against the pre-fix tree it produced exactly the audit's twelve findings, including README:49-50, architecture.md:4, development.md:157, operations.md:123 and both ADR index rows.",
    "LEDGER SEMANTIC PASS: G2's `why` was carrying '24 self-test controls' (26 execute now, measured), and its envelope / ambiguity / S3-evidence statements have been rewritten to say what 09ce01e, 3ac9ae9 and 21780b0 actually established - and, as importantly, what they did not (local enforcement is not provider enforcement; code-level ambiguity closure is not the real-DO probe obligation; a source-authentic provider bundle is not product qualification)."
   ]
  },
  {
   "id": "R6-PR8-HYGIENE",
   "title": "Round-6 R6-HYGIENE-01: formatting, whitespace and warning gates are green and enforced",
   "status": "IN_PROGRESS",
   "items": [
    "ROOT CAUSE of the ~180-hunk rustfmt diff: the tools workspace is WRITTEN in the project's 120-column style but had no rustfmt.toml, so `cargo fmt --check` measured it against rustfmt's 100-column DEFAULTS. tools/rustfmt.toml now pins the same style as fork/typedb/rustfmt.toml (differing only in edition, which follows this workspace's edition 2021), and the workspace is formatted: `cargo +nightly-2026-04-15 fmt --manifest-path tools/Cargo.toml --all -- --check` exits 0.",
    "PATCH WHITESPACE: `git diff --check 29fd003..HEAD` flagged four lines in fork/slatedb/patches/0004 and 0005. Those are diff CONTEXT lines for empty source lines - a unified diff encodes them as a single space, and stripping it would CORRUPT the patch. Fixed with a .gitattributes `-whitespace` attribute for *.patch rather than by editing the patches; their real integrity gate is the post-patch tree digest in tools/fork/materialize_slatedb.py --check. `git diff --check 29fd003..HEAD` and `git diff --check` now both exit 0.",
    "CLIPPY: protocol-models and storage-diff-spike are held at -D warnings in tier-1 CI. storage-diff-spike needed two fixes: manual_is_multiple_of (rewritten) and should_implement_trait / unusual_byte_groupings, both #[allow]ed AT THE SITE with the reason written down - an infinite seeded generator must not become an Iterator, and the differential seed is grouped to read as 'slatedb' and its VALUE is load-bearing for archived evidence.",
    "WARNING RATCHET: `storage` cannot be flipped to -D warnings without editing upstream code we do not own, so tools/ci/warning_ratchet.py enforces a one-way ratchet against tools/ci/warning-baseline.json. Baseline measured 2026-08-20: 13 warnings across 3 lints (9 dead_code, 2 unused_imports, 2 private_interfaces), none in the SlateDB adapter or the recovery path, one of them the INTENTIONAL dead init_writer in the fork's manifest/store.rs that patch 0001 replaced. New lint, increased count, unratcheted improvement and dead baseline entry all FAIL.",
    "OPEN, and NOT done by this pass: `cargo +nightly-2026-04-15 fmt --manifest-path fork/typedb/Cargo.toml --all -- --check` reports 48 hunks across 9 files (storage/keyspace/slate.rs 15, storage/factory.rs 13, storage/recovery/checkpoint.rs 6, storage/tests/test_recovery.rs 5, storage/storage.rs 2, storage/recovery/sha256.rs 2, storage/keyspace/mod.rs 2, database/database.rs 2, durability/wal.rs 1). The tier-1 hygiene job enforces it; the fork owner runs `cargo +nightly-2026-04-15 fmt --manifest-path fork/typedb/Cargo.toml --all` to clear it.",
    "remote-wal-spike clippy: RED when this pass added the gate (derivable_impls at l1_stream.rs:738, field_reassign_with_default at l1_stream.rs:1099, both in the in-flight streaming file), GREEN when re-measured after commit bf748f8 - `cargo +1.93.0 clippy --manifest-path tools/Cargo.toml -p remote-wal-spike --all-targets --locked -- -D warnings` now exits 0. All three tools crates are therefore held at -D warnings in tier-1 CI."
   ]
  },
  {
   "id": "R6-PR8-PORT",
   "title": "Round-6 R6-PORT-01: a source archive can prove its own identity",
   "status": "IN_PROGRESS",
   "items": [
    "MECHANISM: RELEASE-IDENTITY.json (committed as a TEMPLATE carrying literal $Format:%H$ tokens) plus a .gitattributes `export-subst` attribute, so `git archive` expands the release commit at export time; tools/release/release_identity.py --generate writes a fully populated body plus an identity_digest over its canonical form for a release tarball or installed package.",
    "RESOLUTION ORDER, identical in both resolvers: a git checkout is authoritative (it alone can also report dirtiness); otherwise the identity file; otherwise a typed SOURCE_IDENTITY_UNAVAILABLE refusal. Refusing is the point - evidence.ts's current fallback returns the string 'UNKNOWN (git rev-parse HEAD failed)' and lets a run continue with no provenance.",
    "EXECUTED, not asserted: tools/release/release_identity_selftest.py materialises all four postures for real (checkout, `git archive` export, release tarball, installed package) plus two negative postures (a naked copy without .git or an expanded identity; an identity edited after it was digest-bound), and resolves each with BOTH the Python resolver CI uses and the TypeScript resolver the probes call. 6/6 postures, both resolvers.",
    "PROBES-SIDE CHANGE NOT APPLIED (control-plane/probes/** is not this pass's territory): three call sites each need one line replaced, plus one import line, to call resolveReleaseCommit(REPO_ROOT) from tools/release/release-identity.mts instead of shelling out to git. Verified compatible: `tsc --noEmit` passes with the cross-workspace .mts import, and `node --experimental-strip-types` resolves it.",
    "Until those three lines land, the two controls the audit found still fail in a `git archive` materialisation - the mechanism exists and is proven, the call sites are not yet switched."
   ]
  },
  {
   "id": "R8-AUDIT",
   "title": "Round-8 total-quality audit: every P0 and P1, and the P2 set, implemented and executed",
   "status": "IN_PROGRESS",
   "items": [
    "P0-01 / P1-01 STORAGE AUTHORITY. Path SUBSTRINGS no longer grant manifest or staging authority: classify_key() is a closed grammar over the path's SEGMENTS returning a typed KeyClass, staging moved to its own top-level namespace with a minted attempt token, and open_remote applies the prefix FIRST so the authoritative principal cannot be reached around. Multipart part admission now decides state, ownership, supersession and budget under ONE lock and fails CLOSED. Executed: storage lib 247 passed / 0 failed (audit baseline 209), recovery integration 18/18, with a CLASSIFICATION_MATRIX, both §3.6 diagnostics, a hostile-root end-to-end and provider-call-count assertions.",
    "P1-02 RESTORE WITNESS. The 256-key probe sample is gone: an ExpectedReplayWitness absorbs every replayed batch and verifies the whole expected index against the durable keyspaces, so a lost write cannot fall outside a sample. Mandated mutants executed, including the 257th-newest key, a lost tombstone, a last-batch-only flush and a root computed from checkpoint bytes alone.",
    "P0-02 LEDGER TRUTH. One canonical `current` block; a guard that refuses any second mutable home for a current fact, reading a string UNDER ITS OWN KEY (the shape the audit found: a gate state restated as a q_dispositions KEY, which no earlier mutant caught because the guard read only values); generated forbidden-claim reasons; a gate-state reducer over blockers and OPEN owner decisions; and a SEMANTIC verifier that re-runs the producers instead of comparing the document to itself. 31/31 ledger mutants killed, 8/8 semantic mutants killed.",
    "P0-03 / P0-04 / P0-05 CONTROLLER. A clean `fast` verifies the whole tree in every language and ts.tests is a gate; tools/quality/bootstrap.py installs the locked closure hermetically into a repository-owned venv and REFUSES exit 3 otherwise; .quality/campaigns.toml makes Full's campaigns executable or declares them not implemented, and `full --plan` refuses before spending the budget.",
    "P1-07 ONE ENVIRONMENT MODEL. .quality/capabilities.toml declares what each of the 41 gates needs; tools/quality/capabilities.py is the single probe implementation and probes STRUCTURALLY (a header by compiling it, a library by dlopening it, a namespace by entering one); the controller runs it before invoking anything and tools/dev/doctor.py imports the same module, so doctor and the gate agree by construction. A gate absent from the inventory is refused, and a Rust test holds the inventory and the gate catalogue to the same set. 11/11 capability mutants held, each building a REAL denied environment.",
    "P1-06 PYTHON TRUTH. A declared denominator (18 measured / 80 reviewed exclusions / 0 unmeasured of 98), branch floors on changed code with no-regression against a committed baseline, negative-input suites over the verifier primitives, and a normalized mutant manifest that FAILS an uncounted suite. 16 tests -> 124. The floor bit on three modules and the answer was tests: validate_modeq 8% -> 62%.",
    "P2 SET. Portability mutants for a non-default CARGO_HOME and a relative bundle; a deterministic synthetic subject for the cucumber control that had no positive subject; /live and /ready split from /health with a stable error code and correlation id; the GC tool's credential off argv and onto a private config-file FD; one VERSIONED namespace codec shared with the Rust layout instead of copied segment positions; and a stale-number guard over EVERY live document, which found three more restatements the four-path scan never saw.",
    "RE-MEASURED, NOT ASSERTED: every number above was produced by running the thing again on this machine after the change."
   ],
   "commits": [
    "ec32b25"
   ]
  }
 ],
 "q_dispositions": {
  "dirty_read_scan_defaults": "CLOSED_NARROW regression guard (paused-precommit tests kept)",
  "optional_capability_restrictions": "CLOSED_NARROW; broader authorization/receipt/ambiguity tracked under PR4",
  "exact_blob_authority_counters": "PARTIAL; generation/incarnation now checked-canonical, Rust IDs/exhaustion partial",
  "key_count_memo": "CLOSED_NARROW; TTL/object-I/O/singleflight controls OPEN",
  "destructive_open_s3_purge": "APPLICATION_FIXED; provider IAM/delete/lifecycle proof OPEN (platform-blocked)",
  "cache_wipe_error": "FIXED_AT_CALL_SITE with control",
  "rocks_cursor_drop_order": "CLOSED_NARROW; complete forged-lifetime boundary OPEN (S-P1-02)",
  "get_prev_retry_policy": "PARTIAL; terminal branch fail-stops, typed boundary + uploader budget under PR5",
  "post_wal_containment": "PARTIAL; waits bounded this commit, validation-I/O semantics under J.5",
  "session_takeover": "CONTAINED; legacy register excluded from production surface, refusal for unmigrated sessions",
  "production_auth_tenant_boundary": "LOCALLY_CLOSED by R4-PR1 (a8adfff): opaque tenant registry + PROVISION transaction, registry-derived DO routing, private-issuer key hierarchy, managed-surface E2E; live remainder is real managed key provisioning/rotation at deploy time (under SI-G0-3) and the asymmetric-key upgrade (round-5 R5-SEC-03)",
  "historical_migration": "REPAIRED_WITH_FIXTURES this commit (C-P0-06)",
  "batch": "DISABLED_IN_PRODUCTION until G2 authorizes; pre-I/O bounds added",
  "checkpoint": "PARTIAL; enumeration/fsync integrity landed, global-cut protocol OPEN",
  "external_epochs": "SHIPPED AND RATIFIED (R6-TRUTH-01, 2026-08-20). The soft fork is what every crate in the product workspace links: `cargo metadata` resolves slatedb to path sources/slatedb-fork@0.15.0 with external_epoch_required enabled, via a workspace-global [patch.crates-io]. ADR-0012 status is 'accepted (shipped)'; ADR-0001 is SUPERSEDED in full. Enforced by tools/ci/check_dependency_sources.py against the resolved graph. Full PRODUCT integration of the epoch protocol end-to-end remains OPEN (post-G2): shipping the fenced engine is not the same as having exercised it against a real platform.",
  "no_fork_firewall": "NOT SELECTED. Candidate B (the publication firewall over stock SlateDB) is measured and its three spike defects are repaired, and its provider-enforced credential rotation remains a sound storage-side backstop underneath whatever ships - but Candidate A is what ships (ADR-0012, round-6 ratification). Candidate B's remaining unmeasured items are unchanged: provider-credential enforcement, the latency envelope of the gate's preflight reads, and a cooperating client for the typed transition envelope.",
  "evidence_denominator": {
   "historical": true,
   "as_of_commit": "7f2ba55",
   "superseded_by": "current.coverage",
   "disposition": "DENOMINATOR_LANDED by R3-H (7f2ba55): an immutable leaf/profile plan plus an independent verifier chain (R4-D). The round-6 execution numbers this entry used to state inline are SUPERSEDED — current.coverage is the only place a current coverage count may live. What remains true and is not a count: COVERED means an outcome was RECORDED, never that it passed; the U1-classic vs U2-SlateDB leaf differential over the same tree and binaries found 0 regressions, 0 absent-on-candidate and 0 outcome-changed."
  },
  "u2_u2s3_verdict": "HISTORICAL_NON_QUALIFYING",
  "official_driver_parity": {
   "historical": true,
   "as_of_commit": "R6-QUAL-01 (2026-08-20)",
   "superseded_by": "current.drivers",
   "disposition": "EXECUTED_LEAF_PARTIAL (R6-QUAL-01, 2026-08-20). All six rows now carry executed, leaf-level evidence that tools/evidence/verify_drivers.py independently re-derives from the archived bytes: 1,132 official behaviour scenarios across 6 lanes, 1,102 in-plan leaf outcomes, 0 failures, 99/99 mutants killed. The SlateDB backend passes the full official Rust, Python and TypeScript suites identically to RocksDB, and the lane separation rests on the archived on-disk witness _system/backend-spec.marker (kind slatedb-r2 / object-store local-fs vs kind classic), not on an environment variable. The earlier 'blocked on external repositories and harness build-out' premise was false: the driver sources are source-locked and present, and only Bazel is missing. Rows are PARTIAL, never COVERED, because three preconditions remain unmet and are each named in docs/evidence/G1/drivers/driver-row-status.json: the cluster suites need a 3-node replicated deployment plus test certificates (TypeDB CE is single-node); the TypeScript lane's Bazel type surface is not reproducible outside Bazel (upstream emits no index.d.cts by its own BUILD TODO); and the pristine/U0 lane is disk-blocked, measured. The plan JSON itself is NOT edited - its driver_rows keep the NOT_IMPLEMENTED seed because the plan body feeds plan_root 72020aa0..., which archived verdicts pin; status is result-side by design."
  }
 },
 "forbidden_claims": [
  {
   "pattern": "G0.*\\bgreen\\b|\\bgreen\\b.*G0",
   "generated_from": "current.gates.G0"
  },
  {
   "pattern": "[Ee]verything local is done",
   "reason": "false: see ledger actions with status OPEN"
  },
  {
   "pattern": "U3\\.0.*\\bgreen\\b",
   "reason": "no current-tree U3.0 evidence bundle exists; the lane is RED pending product integration (C-P0-01 itself is closed by PR4b/R4-E)"
  },
  {
   "pattern": "105 green",
   "reason": "the U2S3 archive is a historical observation: 105 exited 0, with 2 parsed failed cases; never summarise as green"
  },
  {
   "pattern": "SlateDB is consumed \\*\\*unmodified from crates\\.io\\*\\*|deliberately no fork|SlateDB is never edited",
   "reason": "R6-TRUTH-01: the product workspace resolves slatedb to sources/slatedb-fork through a workspace-global [patch.crates-io]; ADR-0012 supersedes ADR-0001 in full. tools/ci/check_dependency_sources.py re-derives this from cargo metadata."
  },
  {
   "pattern": "\\b24 (self-test )?controls\\b|\\b24/24 controls\\b",
   "reason": "R6-TRUTH-01: the probe harness executes 26 controls (measured 2026-08-20). A stale count in a status document is a stale claim about how much was proved."
  }
 ],
 "adopted_audit_round3": {
  "directive": "TypeDB on SlateDB/R2 round-3 deep audit and total-quality implementation directive, 2026-08-18",
  "audited_commit": "05886cf1dde17b5e07c80684a691e83fb0401a64",
  "decision": "NO-GO for a live Cloudflare run today; continue local implementation",
  "response": "docs/reviews/deep-audit-2026-08-18-round3-response.md"
 },
 "adopted_audit_round4": {
  "directive": "TypeDB on SlateDB/R2 round-4 deep audit and pre-Cloudflare total-quality directive, 2026-08-19",
  "audited_commit": "e1f88a0ee6cd6d43f1fa6eeaa067161f58c2fc2a",
  "decision": "NO-GO for a product-labelled Cloudflare live qualification; continue local implementation. The credentialed probe runner was additionally under a DESTRUCTIVE STOP (R4-CF-00: RED preflight issued a lock-policy reset) until the zero-authority repair landed; the stop is lifted for the repaired runner but the run remains NOT READY pending the R4 program (deployable harness, obligation-manifest assertions, owner envelope, credentials).",
  "local_s3_decision": "OD-009: RustFS strategic target / MinIO loopback-confined transition baseline; promotion gated on the provider-neutral certification corpus",
  "confidentiality_decision": "OD-008 OPEN: no confidentiality profile is decided; v16 §19.6 is NOT closed",
  "response": "docs/reviews/deep-audit-2026-08-19-round4-response.md"
 },
 "adopted_audit_round6": {
  "directive": "TypeDB × SlateDB × Cloudflare R2 round-6 deep audit and implementation directive, 2026-08-20",
  "audited_commit": "29fd0038c0fe1b3ddb6c9baebd6fadf2226c345c",
  "decision": "NO-GO for a live qualification run and NO-GO for a production claim; continue local implementation (R6 program adopted)",
  "note": "This block records ADOPTION of the round-6 directive. It is not a claim that the round-6 program is complete; see the R6-* actions below for per-finding status."
 }
}
