{
  "document": "owner-decision and limits registry (consolidated directive §5.4)",
  "rule": "A value in this file is a product, security or SLO choice the implementation must NOT invent. Each entry records the safe default in force, why that default is safe rather than correct, and what an owner decision would change. An entry stays OPEN until an owner records a decision; while OPEN, no gate may be claimed on the strength of the default.",
  "entries": [
    {
      "id": "OD-001",
      "status": "OPEN",
      "title": "Soak duration and long-run thresholds",
      "question": "How long is the release soak, and what are its pass thresholds?",
      "default_in_force": "None. No soak is claimed anywhere in this repository.",
      "why_safe": "Claiming a soak that was not run is the failure mode; claiming none is merely incomplete. G12 names the long release soak; anything shorter is reconnaissance and is labelled as such.",
      "owner_decision_changes": "The G12 gate definition and the evidence schema for soak runs.",
      "referenced_by": ["docs/operations.md gate table"]
    },
    {
      "id": "OD-002",
      "status": "OPEN",
      "title": "Snapshot-admission watermark wait deadline",
      "question": "How long may snapshot admission wait for the visibility watermark before the database is declared unavailable?",
      "default_in_force": "600 seconds, then fail-stop with diagnostics naming the target and current watermark (fork/typedb/storage/storage.rs, WATERMARK_WAIT_DEADLINE).",
      "why_safe": "The previous behaviour was an unbounded spin: an unresolved commit obligation presented as every later snapshot open hanging with no diagnostic. Any finite bound is safer than none, and 600s is far beyond any healthy wait, so the bound fires only when a slot is genuinely stuck. It is deliberately NOT tuned for throughput.",
      "owner_decision_changes": "The value, and whether the terminal action is a fail-stop or a typed unavailable response (the latter requires changing open_snapshot_* to fallible signatures across the concept/database/query layers).",
      "referenced_by": ["fork/typedb/storage/storage.rs", "fork/typedb/storage/post_wal_guard.rs"]
    },
    {
      "id": "OD-003",
      "status": "OPEN",
      "title": "Data-path object size ceiling",
      "question": "What is the maximum single data-path object the control plane admits?",
      "default_in_force": "8 MiB (control-plane/src/controller/core/capability.ts MAX_CAPABILITY_BYTES, worker-entry.ts MAX_REQUEST_BODY_BYTES), enforced before the body is read and again after.",
      "why_safe": "It is the figure the F9 contract clause names, and it is enforced as a CEILING that a capability may narrow but never widen - so an over-permissive issuer cannot raise it.",
      "owner_decision_changes": "The ceiling, and whether oversized payloads get a chunked/streamed path instead of a refusal.",
      "referenced_by": ["control-plane/src/controller/core/capability.ts"]
    },
    {
      "id": "OD-004",
      "status": "OPEN",
      "title": "Concurrent R2 subrequests per worker invocation",
      "question": "How many concurrent R2 reads may one invocation issue?",
      "default_in_force": "6 (control-plane/src/controller/worker-entry.ts R2_SUBREQUEST_CEILING), taken from the Cloudflare contract lock's 'simultaneous outgoing connections per invocation is six'.",
      "why_safe": "It is the platform's stated hard ceiling rather than a tuned number; the previous value of 8 was a request the platform cannot serve. Reserving headroom below 6 for control connections is a plausible refinement, but lowering it is a performance decision, not a safety one.",
      "owner_decision_changes": "Whether to reserve headroom below the platform ceiling, and how the reservation is accounted when a route makes both DO and R2 calls.",
      "referenced_by": ["contract/typedb-r2-v16-cloudflare-contract-lock.json"]
    },
    {
      "id": "OD-005",
      "status": "OPEN",
      "title": "Flake-ledger expiry policy",
      "question": "How long may a tolerated anomaly stay in docs/evidence/flake-ledger.json before it must be fixed or re-justified?",
      "default_in_force": "Per-entry expiry dates, currently 2027-06-30 for the two live entries; an expired entry is reported and stops tolerating, it does not silently keep excluding.",
      "why_safe": "Both live entries are upstream defects (todo!() stubs and a declared #[ignore]) that fail identically on the RocksDB oracle, so they are baseline-equal rather than port debt. The dates are placeholders chosen to be finite.",
      "owner_decision_changes": "The maximum permitted exclusion lifetime and who may extend one.",
      "referenced_by": ["tools/catalog/verdict.py", "docs/evidence/flake-ledger.json"]
    },
    {
      "id": "OD-006",
      "status": "OPEN",
      "title": "Storage async-bridge deadline and object-store retry bound",
      "question": "How long may a single storage operation wait on the async->sync bridge before fail-stop, and how many times may SlateDB retry a failing object-store call?",
      "default_in_force": "Bridge: report every 30 s (ERROR log naming the pending operation), abort the process at 600 s (fork/typedb/storage/keyspace/slate.rs, bridge()). Object store: object_store_max_retries = Some(8) in SlateKeyspace::settings(), replacing the upstream unbounded default.",
      "why_safe": "The previous behaviour was an unbounded recv() over an unbounded retry loop: a persistent R2/S3 fault presented as a silent forever-hang holding storage locks. Fail-stop with a named operation is the same doctrine as Q-01's guard, and 600 s / 8 retries are far beyond any healthy operation, so the bounds fire only on genuine faults. Neither value is tuned for availability.",
      "owner_decision_changes": "Both values, the report cadence, and whether the terminal action stays a process abort or becomes a typed storage-unavailable error surfaced through fallible read/write signatures.",
      "referenced_by": ["fork/typedb/storage/keyspace/slate.rs", "docs/evidence/G3/controls/storage-containment-controls.txt"]
    }
  ]
}
