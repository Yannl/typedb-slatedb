
# Generated by TypeDB Cargo sync tool.
# Do not modify this file.

# S-02 (round-3) / R5-STOR-04 (round-5): `external_epoch_required` is the
# SlateDB fork's fail-closed fence (an unset external writer epoch becomes a
# refusal at open instead of a silent fallback to internal epoch allocation).
# Round 3 declared it as an OPT-IN crate feature; the round-5 audit correctly
# called that "tested but not shipped" — a test-only feature is not a fence.
# It is now enabled UNCONDITIONALLY on the slatedb dependency below, so the
# ordinary `cargo build -p storage` carries it: every SlateDB open in the
# product refuses without a controller-issued epoch, and slate.rs threads an
# epoch at every builder site so legitimate opens succeed. Provenance check:
# tools/fork/check_strict_epoch.py fails unless `cargo tree -p storage -e
# features` resolves slatedb with external_epoch_required present.
#
# The crate feature is kept as a no-op alias so existing lane invocations
# (cargo ... -p storage --features external_epoch_required) stay valid; it can
# no longer be turned OFF, only redundantly re-asserted.
features = { external_epoch_required = ["slatedb/external_epoch_required"] }

[package]
	name = "storage"
	edition = "2024"
	version = "0.0.0"

[lib]
	path = "storage.rs"
	crate-type = ["lib"]

[dev-dependencies]

	# Arms SlateDB's built-in fail_point! hooks in TEST builds only (feature
	# unification on the fail-parallel crate slatedb already depends on): the
	# committed-frontier read-contract negative control pauses the
	# `write-batch-pre-commit` hook — the exact control V16 anchor 25 names.
	[dev-dependencies.fail-parallel]
		version = "=0.6.0"
		features = ["failpoints"]

	# R8-P2-05: the namespace-schema drift test reads
	# schemas/materialisation-namespace.json and holds it against what this
	# adapter actually writes. TEST-ONLY: the shipped adapter parses no JSON.
	[dev-dependencies.serde_json]
		workspace = true

	[dev-dependencies.test_utils]
		workspace = true

	[dev-dependencies.rand]
		workspace = true

	[dev-dependencies.xoshiro]
		workspace = true

	[dev-dependencies.database]
		workspace = true

	[dev-dependencies.criterion]
		workspace = true

	[dev-dependencies.test_utils_storage]
		workspace = true

	[dev-dependencies.pprof]
		workspace = true

	[dev-dependencies.rand_core]
		workspace = true

[dependencies]

	[dependencies.tracing]
		workspace = true

	[dependencies.primitive]
		workspace = true

	[dependencies.serde]
		workspace = true

	[dependencies.resource]
		workspace = true

	[dependencies.fail_point]
		workspace = true

	[dependencies.logger]
		workspace = true

	[dependencies.durability]
		workspace = true

	[dependencies.error]
		workspace = true

	[dependencies.itertools]
		workspace = true

	[dependencies.diagnostics]
		workspace = true

	[dependencies.bytes]
		workspace = true

	[dependencies.bincode]
		workspace = true

	[dependencies.options]
		workspace = true

	[dependencies.rocksdb]
		workspace = true

	# TB-P7: U2 (SlateDB LocalFS) keyspace engine.
	# R5-STOR-04: `external_epoch_required` is added here ON TOP of the
	# workspace feature set, unconditionally — the strict external-epoch
	# fence is shipped default behavior of every build that links storage,
	# not a test-lane posture.
	[dependencies.slatedb]
		workspace = true
		features = ["external_epoch_required"]

	[dependencies.tokio]
		workspace = true

	# V16 F3: the NoDeleteStore runtime principal implements the
	# object_store trait (async-trait) and its list stream type (futures).
	[dependencies.async-trait]
		workspace = true

	[dependencies.futures]
		workspace = true

	[dependencies.same-file]
		workspace = true

	[dependencies.typeql]
		workspace = true

	[dependencies.chrono]
		workspace = true

	[dependencies.lending_iterator]
		workspace = true

[[bench]]
	name = "bench_rocks"
	harness = false

[[bench]]
	name = "bench_mvcc_storage"
	harness = false

[[test]]
	path = "tests/test_isolation.rs"
	name = "test_isolation"

[[test]]
	path = "tests/test_recovery.rs"
	name = "test_recovery"

[[test]]
	path = "tests/test_snapshot.rs"
	name = "test_snapshot"

[[test]]
	path = "tests/test_mvcc.rs"
	name = "test_mvcc"

[[test]]
	path = "tests/test_storage.rs"
	name = "test_storage"

