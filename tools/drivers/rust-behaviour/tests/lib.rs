// Empty crate root: this package exists only to carry the [[test]] targets
// that project the upstream Bazel rust_behaviour_test rules into cargo.
// The test binaries are built from the locked upstream sources directly.
